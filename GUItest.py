from tkinter import *
user_input = ""





class GUI():
    def __init__(self):
        top = Tk()
        button1 = Button(top, text="Shutdown computer", padx=30,pady=50)
        button2 = Button(top, text="Close down game     ",padx=30,pady=50,command=)
        button1.grid(row=1, column=0)
        button2.grid(row=2, column=0)
        window = Label(top, text="SeedingScript")
        window.mainloop()

    def shutdownbutton(self):
        global
        return
